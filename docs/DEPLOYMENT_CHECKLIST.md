# ✅ Чеклист для развёртывания обновления

## Перед запуском

- [ ] Python 3.10+ установлен
- [ ] Все зависимости установлены (`pip install -r requirements.txt`)
- [ ] ComfyUI запущен и доступен
- [ ] Workflows存在:
  - [ ] `workflows/image_default.json`
  - [ ] `workflows/sdxl_base_refiner.json` (опционально, есть fallback)
  - [ ] `workflows/video_hunyuan15_720p_api.json` (для видео)

## Проверка кода

- [x] `bot/handlers/generate.py` обновлен
  - [x] Удалены ссылки на `image_edit`
  - [x] Добавлены константы WORKFLOW_IMAGE_DEFAULT, WORKFLOW_IMAGE_REFINER
  - [x] Добавлены photo action handlers
  - [x] FSM логика для редактирования
  - [x] Нет синтаксических ошибок

- [x] `comfy/client.py` обновлен
  - [x] Добавлена `resolve_outputs_simple()`
  - [x] Retry логика в `download_file()`
  - [x] Нет breaking changes

- [x] `main.py` обновлен
  - [x] Документация добавлена

## Первый запуск

```bash
# 1. Запустить бота
python main.py

# 2. Проверить логи
tail -f logs/bot.log

# Должны увидеть:
# - "Handlers loaded"
# - "Bot started"
# - Без ошибок
```

## Тестирование в Telegram

### Тест 1: Статус
```
Отправить: /status
Ожидать: Информация о ComfyUI, GPU, VRAM
```

### Тест 2: Улучшение
```
1. Отправить любое фото
2. Ожидать: Появятся 3 кнопки
3. Нажать: ✨ Улучшить
4. Ожидать: "✨ Улучшаю фото…"
5. Ожидать: 30-60 сек
6. Ожидать: Фото приходит с "✅ Готово!"
```

### Тест 3: Редактирование по тексту
```
1. Отправить фото
2. Нажать: ✏️ Редактировать (промпт)
3. Отправить: "измени фон на пляж"
4. Ожидать: "🛠️ Редактирую…"
5. Ожидать: 60-120 сек
6. Ожидать: Отредактированное фото
```

### Тест 4: Редактирование с маской
```
1. Отправить фото
2. Нажать: 🎭 Редактировать (маска + промпт)
3. Отправить: Ч/б маску (белое = менять)
4. Ожидать: "✅ Маска получена…"
5. Отправить: "добавить красный квадрат"
6. Ожидать: 120-180 сек
7. Ожидать: Результат с маской
```

### Тест 5: Видео (убедиться не сломалось)
```
Отправить: "video: кот прыгает"
Ожидать: Видео генерируется как раньше ✅
```

## Проверка логов

```bash
# Успешный путь должен содержать:
grep "Result ready: node=" logs/bot.log
grep "Telegram send:" logs/bot.log

# Не должно быть:
grep "error\|failed\|Error\|Failed" logs/bot.log

# Если есть ошибки, см.:
grep "Image result failed\|Telegram send failed" logs/bot.log
```

## Откат (если что-то сломалось)

```bash
# 1. Остановить бота
Ctrl+C

# 2. Если был backup (если делали):
cp bot/handlers/generate.py.backup bot/handlers/generate.py
cp comfy/client.py.backup comfy/client.py

# 3. Перезапустить
python main.py
```

Время отката: ~2 минуты

## Мониторинг

### Нормальные сообщения в логах
```
"✓ Result ready after X checks: filename=..., size=... bytes"
"Telegram send: photo/video/document"
"Image result: filename=..."
```

### Проблемные сообщения
```
"Image result failed: RuntimeError"
"Send attempt X failed: TelegramBadRequest"
"Timeout waiting for result"
"Mask mode requested but no mask inputs found" (OK - normal)
```

## Критерии успеха

✅ Все 5 тестов пройдены  
✅ Нет error сообщений в логах  
✅ Кнопки появляются после фото  
✅ Результаты приходят в Telegram  
✅ "Result ready: node=X type=Y" в логах  

## Производительность (ожидаемая)

На 8GB VRAM:
- Enhance: 30-60 сек
- Edit: 60-120 сек
- Mask edit: 120-180 сек

Если дольше (2-3x) — ComfyUI перегружен

## Успехи! 🟢

Если всё прошло успешно:
1. Бот готов к использованию
2. Пользователи могут редактировать фото
3. Все результаты доставляются в Telegram

---

**Вопросы?** См. `docs/PHOTO_EDITING_TECH.md`
