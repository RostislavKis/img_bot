## 2026-02-08 — T-003: Справочник workflows + проверка outputs

(T-003) Автогенерация WORKFLOWS_REFERENCE.md через tools/gen_workflows_reference.py (парсинг workflows/*.json).

**Цель:** Зафиксировать список workflows и подготовить reference-документацию. Проверить текущий outputs resolver в comfy/client.py и при необходимости улучшить логирование.

**Тикет:** T-003

**Изменено/добавлено:**
- docs/WORKFLOWS_REFERENCE.md (создан, статический список workflows)
- docs/CHANGELOG_INTERNAL.md (эта запись)

**Примечание:** Детальное описание входов/выходов каждого workflow будет дописано в WORKFLOWS_REFERENCE.md после анализа JSON.

---

# CHANGELOG_INTERNAL (img_bot)

Р–СѓСЂРЅР°Р» Р’РЎР•РҐ РІРЅСѓС‚СЂРµРЅРЅРёС… РёР·РјРµРЅРµРЅРёР№ РїСЂРѕРµРєС‚Р°. Р’СЃРµ Р·Р°РїРёСЃРё РЅР° СЂСѓСЃСЃРєРѕРј.

**РџСЂР°РІРёР»Рѕ:** РєР°Р¶РґС‹Р№ РєРѕРјРјРёС‚ СЃ РёР·РјРµРЅРµРЅРёРµРј РєРѕРґР° РћР‘РЇР—РђРќ СЃРѕРґРµСЂР¶Р°С‚СЊ Р·Р°РїРёСЃСЊ Р·РґРµСЃСЊ.

---

## 2025-01-17 — T-003.2: Усилено логирование outputs + CI-check актуальности WORKFLOWS_REFERENCE

**Цель:** Улучшить диагностику при выборе output файла из ComfyUI, добавить автоматическую проверку актуальности справочника workflows.

**Тикет:** T-003.2

**Обновлено:**

- `comfy/client.py` — структурное логирование при парсинге outputs
  - `_get_file_priority()` — добавлены явные комментарии правил приоритета
    * video (mp4/webm/mov/avi/mkv) → 0 (TOP)
    * gif → 1 (высокий приоритет)
    * image (png/jpg/jpeg/webp/bmp) → 2
    * прочее → 3
  - `_extract_first_file()` — детальное логирование (INFO/DEBUG):
    * 📦 Parsing outputs: сколько нод, какие node_ids
    * 📋 Found N candidate(s): список всех файлов с priority, size, type
    * ✓ Selected output: финальный выбор + reason (type=video/gif/image/other, size)
    * ❌ При отсутствии: warning + available keys + первые 500 символов JSON outputs
  - Правило выбора: сортировка по (priority asc, size desc) — видео > gif > изображение, внутри типа больше = лучше

**Добавлено:**

- `.github/workflows/docs-workflows-reference.yml` — новый GitHub Actions workflow
  - Запускается при push/PR на master/main
  - Перегенерирует `docs/WORKFLOWS_REFERENCE.md` через `python tools/gen_workflows_reference.py`
  - Проверяет актуальность: `git diff --exit-code docs/WORKFLOWS_REFERENCE.md`
  - Если diff найден → CI falls с понятным сообщением
  - Гарантирует что справочник workflows всегда синхронизирован с workflows/*.json

**Результат:**

- Логирование outputs максимально детально (помогает при дебаге OOM, пустых outputs, неправильного выбора файла)
- CI автоматически проверяет что разработчик не забыл обновить WORKFLOWS_REFERENCE.md при добавлении/изменении workflow
- При запросе логов видны: кандидаты, приоритеты, финальный выбор с причиной

---

## 2026-02-08 — T-003: Справочник workflows + проверка outputs

(T-003) Автогенерация WORKFLOWS_REFERENCE.md через tools/gen_workflows_reference.py (парсинг workflows/*.json).

**Цель:** Зафиксировать список workflows и подготовить reference-документацию. Проверить текущий outputs resolver в comfy/client.py и при необходимости улучшить логирование.

**Тикет:** T-003

**Изменено/добавлено:**
- docs/WORKFLOWS_REFERENCE.md (создан, статический список workflows)
- docs/CHANGELOG_INTERNAL.md (эта запись)

**Примечание:** Детальное описание входов/выходов каждого workflow будет дописано в WORKFLOWS_REFERENCE.md после анализа JSON.

---

# CHANGELOG_INTERNAL (img_bot)

Р–СѓСЂРЅР°Р» Р’РЎР•РҐ РІРЅСѓС‚СЂРµРЅРЅРёС… РёР·РјРµРЅРµРЅРёР№ РїСЂРѕРµРєС‚Р°. Р’СЃРµ Р·Р°РїРёСЃРё РЅР° СЂСѓСЃСЃРєРѕРј.

**РџСЂР°РІРёР»Рѕ:** РєР°Р¶РґС‹Р№ РєРѕРјРјРёС‚ СЃ РёР·РјРµРЅРµРЅРёРµРј РєРѕРґР° РћР‘РЇР—РђРќ СЃРѕРґРµСЂР¶Р°С‚СЊ Р·Р°РїРёСЃСЊ Р·РґРµСЃСЊ.

---

## 2025-01-17 — T-003.3: Детерминированный генератор WORKFLOWS_REFERENCE (без timestamp)

**Цель:** Убрать timestamp из генератора справочника, чтобы избежать "пустых" коммитов и рассинхрона в CI.

**Тикет:** T-003.3

**Обновлено:**

- `tools/gen_workflows_reference.py` — полностью переписан для детерминированного вывода
  - Убран timestamp из шапки файла (была строка с датой/временем генерации)
  - Улучшена функция `_normalize_nodes()` — поддержка как dict, так и list формата nodes
  - Улучшена `_guess_kind()` — более надёжная эвристика определения типа (image/video/unknown)
  - Добавлена `_md_escape_cell()` — экранирование символов для Markdown таблиц
  - Детерминированный порядок: сортировка workflows по имени (sorted)
  - Стабильный вывод: одинаковый JSON → одинаковый WORKFLOWS_REFERENCE.md

**Результат:**

- Генератор теперь детерминированный: при одинаковых workflows/*.json выдаёт идентичный WORKFLOWS_REFERENCE.md
- CI `.github/workflows/docs-workflows-reference.yml` не будет ловить ложные изменения
- Коммиты станут чище (не будет diff на одну строку с timestamp)





