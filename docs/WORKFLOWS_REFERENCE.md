# 📚 Справочник по Workflows (img_bot)

Этот файл генерируется скриптом `tools/gen_workflows_reference.py` на основе JSON из папки `workflows/`.

---

## 📋 Сводная таблица

| Workflow | Тип | LoadImage | Mask | Prompt-поля | Выходы (по нодам) |
|---|---:|:---:|:---:|---|---|
| `flux_dev_fp8` | unknown | — | — | — | — |
| `flux_schnell_fp8` | unknown | — | — | — | — |
| `image_default` | unknown | — | — | — | — |
| `sdxl_base_refiner` | unknown | — | — | — | — |
| `video_default` | unknown | — | — | — | — |
| `video_hunyuan15_720p` | image | — | ✅ | — | — |
| `video_hunyuan15_720p_api` | unknown | — | — | — | — |
| `video_hunyuan15_i2v_api` | image | — | ✅ | — | — |
| `video_hunyuan15_t2v_api` | image | — | ✅ | — | — |
| `video_ltx` | image | — | ✅ | — | — |
| `video_sd15` | unknown | — | — | — | — |
| `video_wan21` | image | — | — | — | — |

---

## 📄 Подробности по каждому workflow

### `flux_dev_fp8.json`

- **Тип:** `unknown`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `flux_schnell_fp8.json`

- **Тип:** `unknown`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `image_default.json`

- **Тип:** `unknown`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `sdxl_base_refiner.json`

- **Тип:** `unknown`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_default.json`

- **Тип:** `unknown`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_hunyuan15_720p.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_hunyuan15_720p_api.json`

- **Тип:** `unknown`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_hunyuan15_i2v_api.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_hunyuan15_t2v_api.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_ltx.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_sd15.json`

- **Тип:** `unknown`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---

### `video_wan21.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если требуется.

---
