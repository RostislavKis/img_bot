# 📚 Справочник по Workflows (img_bot)

_Автосгенерировано: 2026-02-09 00:35:06_

Этот файл генерируется скриптом `tools/gen_workflows_reference.py` на основе JSON из папки `workflows/`.

---

## 📋 Сводная таблица

| Workflow | Тип | LoadImage | Mask | Prompt-поля | Выходы (по нодам) |
|---|---:|:---:|:---:|---|---|
| `flux_dev_fp8` | image | — | — | positive, text | 9: SaveImage -> images[] |
| `flux_schnell_fp8` | image | — | — | positive, text | 9: SaveImage -> images[] |
| `image_default` | image | — | — | positive, text | 7: SaveImage -> images[] |
| `sdxl_base_refiner` | image | — | — | positive, text | 7: SaveImage -> images[] |
| `video_default` | video | — | — | positive, text | 7: VHS_VideoCombine -> video |
| `video_hunyuan15_720p` | image | — | ✅ | — | — |
| `video_hunyuan15_720p_api` | image | ✅ | — | positive, text | 102: SaveVideo -> video |
| `video_hunyuan15_i2v_api` | video | — | ✅ | — | — |
| `video_hunyuan15_t2v_api` | video | — | ✅ | — | — |
| `video_ltx` | video | — | ✅ | — | — |
| `video_sd15` | video | — | — | positive, text | 9: VHS_VideoCombine -> video |
| `video_wan21` | video | — | — | — | — |

---

## 📄 Подробности по каждому workflow

### `flux_dev_fp8.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** positive, text
- **Выходы (эвристика по нодам):**
  - 9: SaveImage -> images[]

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `flux_schnell_fp8.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** positive, text
- **Выходы (эвристика по нодам):**
  - 9: SaveImage -> images[]

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `image_default.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** positive, text
- **Выходы (эвристика по нодам):**
  - 7: SaveImage -> images[]

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `sdxl_base_refiner.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** positive, text
- **Выходы (эвристика по нодам):**
  - 7: SaveImage -> images[]

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_default.json`

- **Тип:** `video`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** positive, text
- **Выходы (эвристика по нодам):**
  - 7: VHS_VideoCombine -> video

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_hunyuan15_720p.json`

- **Тип:** `image`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_hunyuan15_720p_api.json`

- **Тип:** `image`
- **LoadImage:** да
- **Mask:** нет
- **Prompt-поля:** positive, text
- **Выходы (эвристика по нодам):**
  - 102: SaveVideo -> video

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_hunyuan15_i2v_api.json`

- **Тип:** `video`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_hunyuan15_t2v_api.json`

- **Тип:** `video`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_ltx.json`

- **Тип:** `video`
- **LoadImage:** нет
- **Mask:** да
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_sd15.json`

- **Тип:** `video`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** positive, text
- **Выходы (эвристика по нодам):**
  - 9: VHS_VideoCombine -> video

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---

### `video_wan21.json`

- **Тип:** `video`
- **LoadImage:** нет
- **Mask:** нет
- **Prompt-поля:** не найдено автоматически
- **Выходы (эвристика по нодам):**
  - не найдено автоматически (проверь ноды сохранения)

**Заметки:** дополни вручную (VRAM, время, точные входы), если потребуется.

---
