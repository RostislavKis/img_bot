# ✅ ЧЕКЛИСТ ТЕСТИРОВАНИЯ HunyuanVideo I2V

## 🚀 ПЕРЕД ЗАПУСКОМ

### 1. Проверка файлов
- [ ] Файл `utils/files.py` существует
- [ ] Файл `config/settings.py` обновлён (содержит `comfy_input_dir` и `tmp_dir`)
- [ ] Файл `bot/handlers/generate.py` обновлён
- [ ] Папка `C:\dev\img_bot\data\tmp` создана
- [ ] Папка `D:\ComfyUI\input` существует

### 2. Конфигурация (опционально)
```bash
# Создать .env.local (если нужно изменить пути):
COMFYUI_INPUT_DIR=D:\ComfyUI\input
BOT_TMP_DIR=C:\dev\img_bot\data\tmp
```

### 3. Запуск служб
```bash
# Терминал 1: ComfyUI
cd D:\ComfyUI
.\run_nvidia_gpu.bat

# Терминал 2: Bot
cd C:\dev\img_bot
python main.py
```

---

## 📝 ТЕСТОВЫЕ СЦЕНАРИИ

### ТЕСТ 1: Видео С ФОТО (базовый функционал)

**Шаги:**
1. Открыть Telegram бота
2. Отправить любое фото (например, фото кота)
3. Дождаться сообщения: `🖼 Фото получено и готово!`
4. Написать: `video: кот прыгает через забор`
5. Дождаться генерации (~1-2 минуты)

**Ожидаемый результат:**
- [ ] Бот отправил сообщение "⏳ Обработка..."
- [ ] В папке `C:\dev\img_bot\data\tmp\` появился файл `tg_photo_*.png`
- [ ] В папке `D:\ComfyUI\input\` появился тот же файл
- [ ] Бот отправил MP4 видео
- [ ] Видео начинается с отправленного фото

**Проверка логов:**
```bash
# В logs/bot.log должно быть:
type logs\bot.log | findstr "inject Hunyuan input image"
# Результат: "✓ inject Hunyuan input image: node=80 file=tg_photo_xxx.png"
```

---

### ТЕСТ 2: Видео БЕЗ ФОТО (автогенерация кадра)

**Шаги:**
1. Открыть Telegram бота
2. НЕ отправлять фото
3. Написать: `video: космический корабль летит через туманность`
4. Дождаться сообщения: `⏳ Фото не получено — генерирую стартовый кадр...`
5. Дождаться сообщения: `✓ Стартовый кадр готов — запускаю видео...`
6. Дождаться генерации (~3-4 минуты)

**Ожидаемый результат:**
- [ ] Бот отправил "⏳ Генерирую стартовый кадр..."
- [ ] Бот отправил "✓ Стартовый кадр готов — запускаю видео..."
- [ ] В папке `C:\dev\img_bot\data\tmp\` появился файл `start_image_*.png`
- [ ] В папке `D:\ComfyUI\input\` появился тот же файл
- [ ] Бот отправил MP4 видео
- [ ] Видео начинается с автосгенерированного кадра

**Проверка логов:**
```bash
type logs\bot.log | findstr "Start image"
# Результат: "Start image saved to tmp: ..."
# Результат: "✓ Start image ready: start_image_xxx.png"
```

---

### ТЕСТ 3: VRAM Presets (проверка fallback)

**Шаги:**
1. Написать: `video: красивый пейзаж`
2. Во время генерации наблюдать за логами ComfyUI

**Ожидаемый результат:**
- [ ] Бот выбрал пресет (360p/480p/720p) по доступному VRAM
- [ ] Если OOM — бот автоматически переключился на более лёгкий пресет

**Проверка логов:**
```bash
type logs\bot.log | findstr "Hunyuan preset attempt"
# Результат: "Hunyuan preset attempt 1/3: 360p"
```

---

### ТЕСТ 4: Обработка ОШИБОК

#### 4.1 Отсутствует LoadImage в workflow
**Шаги:**
1. Временно переименовать node 80 в workflow
2. Попробовать сгенерировать видео

**Ожидаемый результат:**
- [ ] Бот вернул ошибку: `❌ Hunyuan I2V workflow: не найден узел входного изображения (LoadImage)`

#### 4.2 Ошибка генерации стартового кадра
**Шаги:**
1. Временно удалить/переименовать `flux_dev_fp8.json`
2. Написать: `video: тест` (без фото)

**Ожидаемый результат:**
- [ ] Бот вернул ошибку: `❌ Не удалось создать стартовый кадр: <детали>`

#### 4.3 ComfyUI недоступен
**Шаги:**
1. Остановить ComfyUI
2. Попробовать сгенерировать видео

**Ожидаемый результат:**
- [ ] Бот вернул ошибку: `❌ Timeout: ComfyUI не вернул output`

---

### ТЕСТ 5: Редактирование фото (старый workflow)

**Шаги:**
1. Отправить фото дома
2. Написать: `Добавь солнечный закат на небе`

**Ожидаемый результат:**
- [ ] Бот использовал `sdxl_base_refiner` workflow
- [ ] Входное фото применено к workflow
- [ ] Бот отправил отредактированное изображение

---

## 🔍 ПРОВЕРКА ФАЙЛОВОЙ СИСТЕМЫ

### После каждого теста проверить:

#### 1. Временная папка (`C:\dev\img_bot\data\tmp\`)
```bash
dir C:\dev\img_bot\data\tmp\
```
**Ожидается:**
- `tg_photo_*.png` — фото от пользователя
- `start_image_*.png` — автосгенерированные кадры

#### 2. ComfyUI input (`D:\ComfyUI\input\`)
```bash
dir D:\ComfyUI\input\
```
**Ожидается:**
- Копии файлов из tmp
- Файлы должны существовать и быть > 1KB

#### 3. ComfyUI output (`D:\ComfyUI\output\`)
```bash
dir D:\ComfyUI\output\*.mp4
```
**Ожидается:**
- Сгенерированные MP4 видео

---

## 📊 МЕТРИКИ ПРОИЗВОДИТЕЛЬНОСТИ

### Время генерации (ориентировочно):

#### С фото от пользователя:
- **360p (8GB GPU):** ~30-60 секунд
- **480p (12GB GPU):** ~60-90 секунд
- **720p (16GB+ GPU):** ~90-120 секунд

#### Без фото (с автогенерацией):
- **Генерация кадра (flux_dev):** ~30-60 секунд
- **I2V 360p:** ~30-60 секунд
- **Всего:** ~60-120 секунд (1-2 минуты)

### Использование VRAM:
- **360p:** ~7-8 GB
- **480p:** ~9-11 GB
- **720p:** ~12-14 GB

---

## 🐛 ДИАГНОСТИКА ПРОБЛЕМ

### Проблема: Файл не копируется в ComfyUI input

**Проверка:**
```bash
# Проверить права доступа
icacls D:\ComfyUI\input

# Проверить что папка существует
dir D:\ComfyUI\input
```

**Решение:**
```bash
# Создать папку если не существует
mkdir D:\ComfyUI\input

# Дать права на запись
icacls D:\ComfyUI\input /grant Users:F
```

---

### Проблема: Бот не видит фото

**Проверка:**
```python
# В логах должно быть:
type logs\bot.log | findstr "Telegram photo saved"
```

**Решение:**
- Убедиться что папка `data\tmp` существует
- Проверить права на запись

---

### Проблема: ComfyUI не находит входное изображение

**Проверка:**
```bash
# Проверить что файл скопирован
dir D:\ComfyUI\input\tg_photo_*.png
```

**Решение:**
- Убедиться что `COMFYUI_INPUT_DIR` указывает на правильную папку
- Проверить логи: `type logs\bot.log | findstr "inject Hunyuan input image"`

---

## ✅ ИТОГОВЫЙ ЧЕКЛИСТ

### Базовая функциональность:
- [ ] ТЕСТ 1: Видео с фото — ✅
- [ ] ТЕСТ 2: Видео без фото (автогенерация) — ✅
- [ ] ТЕСТ 3: VRAM presets работают — ✅
- [ ] ТЕСТ 4: Ошибки обрабатываются корректно — ✅
- [ ] ТЕСТ 5: Редактирование фото работает — ✅

### Файловая система:
- [ ] Файлы сохраняются в `data/tmp` — ✅
- [ ] Файлы копируются в `D:\ComfyUI\input` — ✅
- [ ] Видео генерируются в `D:\ComfyUI\output` — ✅

### Логи:
- [ ] Логи содержат `"✓ inject Hunyuan input image"` — ✅
- [ ] Логи содержат `"Start image ready"` (для автогена) — ✅
- [ ] Логи содержат `"Hunyuan preset attempt"` — ✅

### UX:
- [ ] Сообщения пользователю понятные — ✅
- [ ] Ошибки описательные — ✅
- [ ] Прогресс виден на каждом шаге — ✅

---

## 🎯 КРИТЕРИИ УСПЕХА

✅ **ПОЛНОСТЬЮ РАБОЧИЙ** если:
- Все 5 тестовых сценариев проходят успешно
- Файлы корректно сохраняются и копируются
- Видео генерируется с правильным входным кадром
- Автогенерация работает без фото от пользователя
- Ошибки обрабатываются понятно

---

**Дата:** 08.02.2026  
**Версия:** 2.0  
**Статус тестирования:** ✅ ГОТОВО К ПРОВЕРКЕ

🚀 Начинайте тестирование с ТЕСТА 1!
