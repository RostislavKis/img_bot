# ✅ ЧЕКЛИСТ ПРОВЕРКИ ВСЕ РАБОТАЕТ

## Предварительная подготовка

- [ ] ComfyUI запущен: `D:\ComfyUI\main.exe`
- [ ] HTTP здоров: `curl http://127.0.0.1:8188/system_stats` или откройте в браузере
- [ ] Бот запущен: `.\.venv\Scripts\python.exe .\main.py`
- [ ] Логи читаны: `tail -f logs/bot.log` (или открой файл в редакторе)

---

## ТЕСТ 1: Базовая функциональность

### Шаг 1: /start → выбор языка

```
1. Откройте чат с ботом в Telegram
2. Напишите /start
3. ОЖИДАНИЕ: две кнопки "🇷🇺 Русский" и "🇬🇧 English"
4. ✅ УСПЕХ: кнопки появились
```

**Если не работает:**
- Проверь логи: `logs/bot.log` (ошибка в start.py?)
- Проверь BOT_TOKEN в .env.local (правильный?)
- Проверь сеть (может быть блокировка?)

---

### Шаг 2: Выбор языка → главное меню

```
1. Кликни на "🇷🇺 Русский"
2. ОЖИДАНИЕ: главное меню с кнопками
   📸 Картинка | ⚡ Быстро
   🎬 Видео    | ✏️ Редактировать
   📖 Справка
   ⚙️ Настройки

3. ✅ УСПЕХ: меню появилось по-русски
```

**Если текст на кракозябрах:**
- Убедись .env.local и все Python-файлы в UTF-8 (без BOM)
- Откройте `bot/keyboards/main_menu.py` в VS Code → правый нижний угол → выбери "UTF-8"
- Пересохрани файл

---

## ТЕСТ 2: Справка и настройки

### Кнопка "📖 Справка"

```
1. Кликни на "📖 Справка"
2. ОЖИДАНИЕ: длинный текст с примерами промптов
   - 📸 КАРТИНКА (Режим DEV)
   - ⚡ БЫСТРО (Режим Schnell)
   - 🎬 ВИДЕО (AnimateDiff)
   - ✏️ РЕДАКТИРОВАНИЕ
   - 💡 ПРАВИЛА ХОРОШЕГО ПРОМПТА
   - 📝 ПРИМЕРЫ ПРОМПТОВ

3. ✅ УСПЕХ: всё по-русски, без кракозябров
```

### Кнопка "⚙️ Настройки"

```
1. Кликни на "⚙️ Настройки"
2. ОЖИДАНИЕ: текущие параметры
   - FLUX DEV: 28 steps, CFG 1.0
   - FLUX Schnell: 4 steps, CFG 1.0
   - SDXL: 20 steps, CFG 7.5
   - Video: 20 steps, CFG 7.0

3. ✅ УСПЕХ: всё видно
```

---

## ТЕСТ 3: Генерация FLUX DEV (качество)

### Режим: Картинка

```
1. В главном меню кликни на "📸 Картинка"
2. Бот напишет: "📸 Режим: Картинка (DEV)"
3. Напиши промпт: красивая девушка в аниме-стиле
4. ОЖИДАНИЕ: статус "⏳ Обработка..."
   → бот загружает workflow
   → вставляет промпт
   → отправляет в ComfyUI
   → ждёт ~2 минуты

5. ✅ УСПЕХ: получено изображение PNG/JPG
   Бот отправляет фото в чат + "✅ Готово!" + меню
```

**Если ошибка "ComfyUI отклонил workflow":**
- Смотри логи ComfyUI (D:\ComfyUI console)
- Проверь: есть ли файл `workflows/flux_dev_fp8.json`?
- Проверь: есть ли модель `D:\ComfyUI\models\checkpoints\flux1-dev-fp8.safetensors`?

**Если ошибка "В workflow не найдено текстовое поле":**
- Смотри логи: `logs/bot.log` → найди строку "Доступные строковые keys: ..."
- Это значит, workflow JSON использует необычную структуру
- Открой `workflows/flux_dev_fp8.json` и проверь, есть ли `inputs.text` в какой-то ноде

---

## ТЕСТ 4: Генерация FLUX Schnell (быстро)

### Режим: Быстро

```
1. В главном меню кликни на "⚡ Быстро"
2. Бот напишет: "⚡ Режим: Быстро (Schnell)"
3. Напиши промпт: кот спит на диване
4. ОЖИДАНИЕ: ~30 секунд (вместо 2 минут)

5. ✅ УСПЕХ: быстрое изображение, качество немного хуже чем DEV
```

**Если не работает:**
- Проверь файл `workflows/flux_schnell_fp8.json`
- Проверь модель `D:\ComfyUI\models\checkpoints\flux1-schnell-fp8.safetensors`

---

## ТЕСТ 5: Генерация SDXL (фото-реализм)

### Новый режим: Фото (SDXL)

```
1. В главном меню кликни на "📸 Картинка"
   (Это DEV по умолчанию)
2. Напиши: xl: профессиональная фотография рыжей кошки
   или: photo: кошка в студийном свете

3. ОЖИДАНИЕ: ~1 минуты
   → workflow: sdxl_base_refiner
   → модель: RealVisXL или JuggernautXL

4. ✅ УСПЕХ: фото-реалистичное изображение
```

**Если ошибка "ComfyUI отклонил workflow":**
- Проверь файл `workflows/sdxl_base_refiner.json` (новый файл!)
- Проверь модели:
  - `D:\ComfyUI\models\checkpoints\RealVisXL_V4.0.safetensors` ИЛИ
  - `D:\ComfyUI\models\checkpoints\JuggernautXL_v6.safetensors`
- Если нет → открой ComfyUI Manager и установи одну из них

---

## ТЕСТ 6: Генерация видео (AnimateDiff)

### Режим: Видео

```
1. В главном меню кликни на "🎬 Видео"
2. Бот напишет: "🎬 Режим: Видео"
3. Напиши промпт: кот медленно идёт по траве, солнечный день
4. ОЖИДАНИЕ: ~1-2 минуты

5. ✅ УСПЕХ: GIF-файл с анимацией
   Бот отправляет документ (video.gif или similar)
```

**Если ошибка:**
- Проверь файл `workflows/video_default.json`
- Проверь модели AnimateDiff установлены в ComfyUI

---

## ТЕСТ 7: Редактирование (Inpainting)

### Режим: Редактирование

```
1. В главном меню кликни на "✏️ Редактировать"
2. Бот напишет: "✏️ Режим: Редактирование"
3. Отправь своё фото (PNG или JPG)
4. Бот ответит: "🖼 Фото получено. Теперь напиши промпт:"
5. Напиши: Измени фон на пляж
   или: Добавь солнечные очки

6. ОЖИДАНИЕ: ~1 минуты (workflow: sdxl_base_refiner с LoadImage)

7. ✅ УСПЕХ: отредактированное изображение
```

**Если ошибка "Этот workflow не поддерживает входное изображение":**
- Проверь workflow JSON: должна быть нода LoadImage с `inputs.image`
- Проверь `workflows/image_default.json` или `workflows/sdxl_base_refiner.json`

---

## ТЕСТ 8: Префиксы (альтернативный способ)

```
БЕЗ кнопок можно писать прямо префиксы:
• "dev: промпт" → FLUX DEV
• "schnell: промпт" или "fast: промпт" → FLUX Schnell
• "photo: промпт" или "xl: промпт" → SDXL
• "video: промпт" или "vid: промпт" → Video
• "Просто промпт" → FLUX DEV (по умолчанию)

1. Напиши: schnell: красивое сияющее озеро
2. ✅ УСПЕХ: быстрая генерация озера
```

---

## ТЕСТ 9: Логирование и отладка

### Проверь логи

```bash
# Windows PowerShell
Get-Content -Tail 50 .\logs\bot.log

# Или откройте файл: logs/bot.log в редакторе
```

**Ищи в логах:**
- `BOT_TOKEN loaded: True` — токен загружен
- `Workflow loaded: flux_dev_fp8, nodes count: 7` — workflow загружен
- `Промпт вставлен в 2 поле(ей)` — промпт вставлен в оба энкодера (FLUX)
- `Checkpoint chosen: flux1-dev-fp8.safetensors` — модель выбрана
- `Result received: ComfyUI_00001.png` — результат получен

**Ошибки в логах:**
- `⚠️ Инжект промпта не найди текстовые поля` → проблема с workflow JSON
- `ComfyUI rejected workflow: ...` → смотри ошибку ComfyUI
- `Timeout: ComfyUI не вернул output` → генерация затянулась или ComfyUI зависла

---

## ТЕСТ 10: UTF-8 (защита от кракозябров)

### Проверь кодировку файлов

```bash
# Windows PowerShell
file bot/handlers/generate.py
file bot/keyboards/main_menu.py
file bot/handlers/help.py
```

**Или в VS Code:**
- Откройте каждый файл
- Нижний правый угол: должно быть "UTF-8" (без BOM)
- Если написано что-то другое → кликни → выбери "UTF-8"

---

## ИТОГОВЫЙ ЧЕКЛИСТ

| Тест | Статус | Комментарий |
|------|--------|-----------|
| /start и выбор языка | ☐ | Кнопки появляются? |
| Главное меню (4 кнопки) | ☐ | По-русски, без кракозябров? |
| Справка (📖) | ☐ | Примеры видны? |
| Настройки (⚙️) | ☐ | Параметры видны? |
| FLUX DEV (качество) | ☐ | ~2 мин, результат получен? |
| FLUX Schnell (быстро) | ☐ | ~30 сек, быстро? |
| SDXL (фото-реализм) | ☐ | "photo:" работает? |
| Video (AnimateDiff) | ☐ | GIF генерируется? |
| Редактирование (фото) | ☐ | Inpainting работает? |
| Префиксы (alt mode) | ☐ | "schnell: промпт" работает? |
| Логи (INFO уровень) | ☐ | Видны ли результаты? |
| UTF-8 (русский текст) | ☐ | Кракозябров нет? |

---

## Быстрая диагностика

### Если ВООБЩЕ ничего не работает:

```bash
# 1. Проверь ComfyUI
curl http://127.0.0.1:8188/system_stats
# Должен вернуть JSON, не ошибку

# 2. Проверь бот
.\.venv\Scripts\python.exe .\main.py
# В консоли должно появиться: "Started polling Telegram API"

# 3. Проверь токен
echo $env:BOT_TOKEN
# Должно быть что-то вроде "1234567:ABCdefXYZ..."

# 4. Смотри логи
Get-Content -Wait .\logs\bot.log
```

---

## Контакты для помощи

- **ComfyUI ошибки**: смотри консоль ComfyUI (D:\ComfyUI\main.exe)
- **Бот ошибки**: смотри `logs/bot.log`
- **Кодировка проблемы**: проверь .editorconfig и VS Code settings (UTF-8)
- **Модели отсутствуют**: установи через ComfyUI Manager или скачай вручную

---

Good luck! 🚀
