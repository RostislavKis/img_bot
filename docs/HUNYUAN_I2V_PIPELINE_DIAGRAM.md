# 🎬 HunyuanVideo I2V — Визуальная диаграмма пайплайна

## СЦЕНАРИЙ A: С ФОТО ОТ ПОЛЬЗОВАТЕЛЯ

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 1. ПОЛЬЗОВАТЕЛЬ                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  [отправляет фото кота]                                                 │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 2. БОТ: msg_photo handler                                               │
├─────────────────────────────────────────────────────────────────────────┤
│  → message.photo[-1]                                                    │
│  → download bytes                                                       │
│  → save_telegram_photo(bytes, tmp_dir)                                  │
│     └─> C:\dev\img_bot\data\tmp\tg_photo_a1b2c3d4.png                  │
│  → ensure_comfy_input_image(tmp_file, comfy_input_dir)                  │
│     └─> D:\ComfyUI\input\tg_photo_a1b2c3d4.png                         │
│  → state.update_data(input_image_comfy_name="tg_photo_a1b2c3d4.png")   │
│                                                                         │
│  📢 "🖼 Фото получено и готово!"                                        │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 3. ПОЛЬЗОВАТЕЛЬ                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  "video: кот прыгает через забор"                                       │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 4. БОТ: msg_prompt handler (HunyuanVideo блок)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  → workflow_name = "video_hunyuan15_720p_api"                           │
│  → prompt = "кот прыгает через забор"                                   │
│  → input_image_comfy_name = st.get("input_image_comfy_name")            │
│     └─> "tg_photo_a1b2c3d4.png" ✓                                       │
│                                                                         │
│  → validate_input_image("tg_photo_a1b2c3d4.png", comfy_input_dir)       │
│     └─> True ✓                                                          │
│                                                                         │
│  → _inject_hunyuan_input_image(wf, "tg_photo_a1b2c3d4.png")             │
│     └─> workflow["80"]["inputs"]["image"] = "tg_photo_a1b2c3d4.png"     │
│     └─> LOG: "✓ inject Hunyuan input image: node=80 file=..."           │
│                                                                         │
│  → _inject_hunyuan_i2v_prompts(wf, prompt)                              │
│     └─> workflow["44"]["inputs"]["text"] = "кот прыгает через забор"    │
│     └─> workflow["93"]["inputs"]["text"] = ""                           │
│                                                                         │
│  → _pick_hunyuan_preset_index(system_stats)                             │
│     └─> start_idx = 0 (360p для 8GB)                                    │
│                                                                         │
│  → for preset in [360p, 480p, 720p]:                                    │
│     → _inject_hunyuan_i2v_settings(wf, preset)                          │
│     → _inject_hunyuan_noise_seed(wf)                                    │
│     → client.queue_prompt(wf) → prompt_id                               │
│     → client.wait_for_result(prompt_id, timeout=600)                    │
│       └─> result = {bytes: ..., filename: "output.mp4"}                 │
│       └─> SUCCESS ✓                                                     │
│                                                                         │
│  📢 "⏳ Обработка... (preset: 360p)"                                    │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 5. COMFYUI                                                              │
├─────────────────────────────────────────────────────────────────────────┤
│  → Получает workflow с:                                                 │
│     • node 80: LoadImage "tg_photo_a1b2c3d4.png"                        │
│     • node 44: positive prompt "кот прыгает через забор"                │
│     • node 93: negative prompt ""                                       │
│     • node 78: width=640, height=360, num_frames=25, fps=12             │
│     • node 127: noise_seed=123456789                                    │
│                                                                         │
│  → Обрабатывает ~1-2 минуты                                             │
│  → Сохраняет: D:\ComfyUI\output\video_00001_.mp4                       │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 6. БОТ: Результат                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  → message.answer_video(result["bytes"], caption="✅ Готово!")          │
│  📢 [MP4 видео отправлено пользователю]                                 │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## СЦЕНАРИЙ B: БЕЗ ФОТО (АВТОГЕНЕРАЦИЯ)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 1. ПОЛЬЗОВАТЕЛЬ                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  "video: космический корабль летит через туманность"                    │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 2. БОТ: msg_prompt handler (HunyuanVideo блок)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  → workflow_name = "video_hunyuan15_720p_api"                           │
│  → prompt = "космический корабль летит через туманность"                │
│  → input_image_comfy_name = st.get("input_image_comfy_name")            │
│     └─> None ✗                                                          │
│                                                                         │
│  📢 "⏳ Фото не получено — генерирую стартовый кадр..."                 │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 3. БОТ: _generate_start_image()                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  → loader.load("flux_dev_fp8")                                          │
│  → _inject_prompt(wf, "космический корабль летит через туманность")     │
│  → _inject_seed_steps_cfg(wf, steps=20, cfg=1.0)                        │
│  → _inject_resolution(wf, width=1024, height=1024)                      │
│  → _choose_checkpoint(available, "flux_dev_fp8", current)               │
│     └─> "flux1-dev-fp8.safetensors" ✓                                   │
│                                                                         │
│  → client.queue_prompt(wf) → prompt_id                                  │
│  → client.wait_for_result(prompt_id, timeout=600)                       │
│     └─> result = {bytes: ..., filename: "image.png"}                    │
│                                                                         │
│  → tmp_file = tmp_dir / "start_image_abc123.png"                        │
│  → tmp_file.write_bytes(result["bytes"])                                │
│     └─> C:\dev\img_bot\data\tmp\start_image_abc123.png                 │
│                                                                         │
│  → ensure_comfy_input_image(tmp_file, comfy_input_dir)                  │
│     └─> D:\ComfyUI\input\start_image_abc123.png                        │
│                                                                         │
│  → return "start_image_abc123.png" ✓                                    │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 4. БОТ: Продолжение msg_prompt                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  → input_image_comfy_name = "start_image_abc123.png" ✓                  │
│                                                                         │
│  📢 "✓ Стартовый кадр готов — запускаю видео..."                        │
│                                                                         │
│  → _inject_hunyuan_input_image(wf, "start_image_abc123.png")            │
│     └─> workflow["80"]["inputs"]["image"] = "start_image_abc123.png"    │
│                                                                         │
│  → _inject_hunyuan_i2v_prompts(wf, prompt)                              │
│  → _pick_hunyuan_preset_index(system_stats)                             │
│  → for preset in [360p]:                                                │
│     → _inject_hunyuan_i2v_settings(wf, preset)                          │
│     → client.queue_prompt(wf) → prompt_id                               │
│     → client.wait_for_result(prompt_id)                                 │
│       └─> result = {bytes: ..., filename: "output.mp4"}                 │
│                                                                         │
│  📢 "⏳ Обработка... (preset: 360p)"                                    │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 5. COMFYUI (HunyuanVideo)                                               │
├─────────────────────────────────────────────────────────────────────────┤
│  → Получает workflow с:                                                 │
│     • node 80: LoadImage "start_image_abc123.png"                       │
│     • node 44: positive prompt "космический корабль..."                 │
│     • node 93: negative prompt ""                                       │
│     • node 78: width=640, height=360, num_frames=25, fps=12             │
│                                                                         │
│  → Обрабатывает ~1-2 минуты                                             │
│  → Сохраняет: D:\ComfyUI\output\video_00002_.mp4                       │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 6. БОТ: Результат                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  → message.answer_video(result["bytes"], caption="✅ Готово!")          │
│  📢 [MP4 видео отправлено пользователю]                                 │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## СТРУКТУРА WORKFLOW NODE 80

```json
{
  "80": {
    "inputs": {
      "image": "tg_photo_a1b2c3d4.png"  ← ИНЖЕКТИМ ЗДЕСЬ
    },
    "class_type": "LoadImage",
    "_meta": {
      "title": "Загрузить изображение"
    }
  },
  "79": {
    "inputs": {
      "crop": "center",
      "clip_vision": ["81", 0],
      "image": ["80", 0]  ← ИСПОЛЬЗУЕТ OUTPUT NODE 80
    },
    "class_type": "CLIPVisionEncode"
  },
  "78": {
    "inputs": {
      "start_image": ["80", 0],  ← ИСПОЛЬЗУЕТ OUTPUT NODE 80
      "clip_vision_output": ["79", 0]
    },
    "class_type": "HunyuanVideo15ImageToVideo"
  }
}
```

---

## ФАЙЛОВАЯ СТРУКТУРА

```
C:\dev\img_bot\
├── data\
│   └── tmp\                              ← временные файлы
│       ├── tg_photo_a1b2c3d4.png         ← фото от пользователя
│       └── start_image_abc123.png        ← автоген кадр
│
├── workflows\
│   ├── video_hunyuan15_720p_api.json     ← HunyuanVideo I2V
│   └── flux_dev_fp8.json                 ← для автогена кадра
│
└── utils\
    └── files.py                          ← утилиты копирования

D:\ComfyUI\
└── input\                                ← ComfyUI читает отсюда
    ├── tg_photo_a1b2c3d4.png             ← скопировано из tmp
    └── start_image_abc123.png            ← скопировано из tmp
```

---

## КЛЮЧЕВЫЕ ФУНКЦИИ

### 1. save_telegram_photo()
```
INPUT:  bytes (фото из Telegram)
OUTPUT: Path (C:\dev\img_bot\data\tmp\tg_photo_xxx.png)
```

### 2. ensure_comfy_input_image()
```
INPUT:  Path (локальный файл)
OUTPUT: str (basename в D:\ComfyUI\input\)
ACTION: copy file to ComfyUI input
```

### 3. validate_input_image()
```
INPUT:  str (filename), Path (comfy_input_dir)
OUTPUT: bool (True если валидный)
CHECKS:
  - not placeholder (blank.png, empty.png)
  - exists in comfy_input_dir
  - size > 1KB
```

### 4. _inject_hunyuan_input_image()
```
INPUT:  dict (workflow), str (filename)
OUTPUT: None
ACTION: workflow["80"]["inputs"]["image"] = filename
RAISES: RuntimeError if LoadImage not found
```

### 5. _generate_start_image()
```
INPUT:  client, loader, prompt, settings, tmp_dir, comfy_input_dir
OUTPUT: str (filename в ComfyUI input)
STEPS:
  1. Load flux_dev_fp8 workflow
  2. Inject prompt
  3. Generate image (1024x1024, steps=20)
  4. Save to tmp
  5. Copy to ComfyUI input
  6. Return basename
```

---

## ВРЕМЕННАЯ ШКАЛА (Сценарий B: автогенерация)

```
t=0s    User: "video: кот прыгает"
        ↓
t=1s    Bot: "⏳ Генерирую стартовый кадр..."
        ↓
t=2s    ComfyUI: загрузка flux_dev_fp8 workflow
        ↓
t=5-60s ComfyUI: генерация стартового кадра (1024x1024)
        ↓
t=61s   Bot: сохранение в tmp → копирование в input
        Bot: "✓ Стартовый кадр готов — запускаю видео..."
        ↓
t=62s   ComfyUI: загрузка HunyuanVideo workflow
        ↓
t=65s   ComfyUI: обработка I2V (preset 360p)
        ↓
t=90s   ComfyUI: сохранение MP4
        ↓
t=91s   Bot: отправка видео пользователю
        Bot: "✅ Готово!"
```

**Общее время:** ~90 секунд (1.5 минуты)  
**Из них:**
- Автоген кадра: ~60 секунд
- I2V генерация: ~30 секунд

---

## СРАВНЕНИЕ ДО/ПОСЛЕ

### ДО (было):
```
User: "video: кот прыгает"
Bot: ⏳ Обработка...
ComfyUI: ERROR - node 80 LoadImage: file not found
Bot: ❌ Ошибка: workflow execution failed
```

### ПОСЛЕ (стало):
```
User: "video: кот прыгает"
Bot: ⏳ Генерирую стартовый кадр...
Bot: ✓ Стартовый кадр готов — запускаю видео...
Bot: ⏳ Обработка...
Bot: ✅ Готово! [MP4 видео]
```

---

**Документация:** docs/HUNYUAN_I2V_PIPELINE_DIAGRAM.md  
**Дата:** 08.02.2026  
**Версия:** 2.0
