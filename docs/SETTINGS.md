# ⚙️ Система настроек img_bot

## Обзор

На ШАГ 3 реализована полная система управления параметрами генерации прямо из бота. Пользователь может настроить:
- Качество генерации
- Соотношение сторон (aspect ratio)
- Seed (для повторяемости)
- Количество шагов (steps)
- CFG / Guidance (точность следования промпту)

Все настройки **сохраняются в БД** и применяются при следующей генерации.

---

## 📋 Параметры

### 1. Качество (Quality)

| Режим | Время | Steps | CFG | Использование |
|-------|-------|-------|-----|----------------|
| **⚡ Быстро** | ~30 сек | 4 | 1.0 | Наброски, много вариантов |
| **⚖️ Баланс** | ~1 мин | 20 | 7.5 | Обычная генерация (по умолчанию) |
| **🎨 Высокое** | ~2 мин | 28 | 7.5 | Качество, детали, реализм |

**Как выбрать:**
- ⚡ Быстро → когда хочешь много вариантов быстро
- ⚖️ Баланс → универсальный выбор на все случаи
- 🎨 Высокое → когда важно качество и можешь подождать

---

### 2. Соотношение сторон (Aspect Ratio)

| Аспект | Размер | Использование |
|--------|--------|----------------|
| **1:1** | 1024×1024 | Квадратные картинки, иконки, аватары |
| **4:5** | 819×1024 | Портреты (вертикально) |
| **16:9** | 1365×768 | Панорамы, пейзажи (горизонтально) |
| **9:16** | 576×1024 | Видео, вертикально (для мобильного) |

**Примеры использования:**
- Портрет девушки → 4:5
- Пейзаж горы → 16:9
- Иконка для приложения → 1:1
- Сторис в Telegram → 9:16

---

### 3. Seed (Повторяемость)

**Seed** — это число, которое определяет "начальное состояние" генератора.

#### 🎲 Случайный seed
- Каждый раз генерируется новое случайное число
- Результат всегда новый и непредсказуемый
- Используй когда хочешь максимум вариантов

#### 📌 Фиксированный seed
- Ты вводишь конкретное число (например, 42)
- Одинаковый seed + одинаковый промпт = одинаковый результат
- Полезно для:
  - Экспериментов (например, менять только одно слово в промпте)
  - Воспроизведения удачного результата
  - Сравнения эффектов разных параметров

**Пример:**
```
Промпт: "красивая девушка, портрет"
Seed: 12345
→ Генерируется девушка A

Промпт: "красивая девушка, портрет" (тот же!)
Seed: 12345 (тот же!)
→ Генерируется девушка A (точная копия!)

Промпт: "красивая девушка, портрет" (тот же!)
Seed: 54321 (другой!)
→ Генерируется девушка B (совсем другая!)
```

---

### 4. Шаги (Steps)

**Steps** — это количество итераций алгоритма генерации. Больше шагов = лучше качество, но медленнее.

| Шаги | Время | Качество | Когда использовать |
|------|-------|----------|-------------------|
| **4** | ~10 сек | Низкое | Быстрая генерация, черновик |
| **20** | ~1 мин | Хорошее | Стандартная генерация |
| **28** | ~2 мин | Отличное | Когда нужны детали |

**Рекомендации:**
- Начни с 20 шагов (баланс между скоростью и качеством)
- Если результат рыхлый или размытый → увеличь до 28
- Если генерация медленная → сни на 4

---

### 5. CFG / Guidance (Точность следования промпту)

**CFG** (Classifier-Free Guidance) — насколько строго модель должна следовать описанию в промпте.

| CFG | Поведение | Когда использовать |
|-----|-----------|-------------------|
| **1.0 🔅** | Модель игнорирует промпт, генерирует творчески | Когда хочешь неожиданного результата, экспериментов |
| **7.5 🔆** | Баланс между творчеством и точностью (по умолчанию) | Для большинства случаев |
| **15.0 🔆** | Модель очень строго следует промпту | Когда нужен точный результат, детальное описание |

**Примеры:**
- CFG=1.0 + промпт "красивая девушка" → может сгенерировать кого угодно, даже животное
- CFG=7.5 + промпт "красивая девушка" → генерирует девушку, но модель может добавить свой стиль
- CFG=15.0 + промпт "красивая девушка в красном платье, портрет, фотография" → генерирует ровно то, что описано

---

## 🎮 Как менять настройки

### В Telegram боте:

1. Нажми кнопку **⚙️ Настройки** в главном меню
2. Увидишь текущие значения:
   ```
   ⚙️ ТЕКУЩИЕ НАСТРОЙКИ
   
   🎨 Качество: ⚖️ Баланс
   📐 Соотношение: 1:1
   🎲 Seed: 🎲 Случайный
   📊 Шаги (steps): 20
   🔆 CFG/Guidance: 7.5
   ```
3. Нажми на то, что хочешь изменить:
   - **🎨 Качество** → выбираешь из 3 вариантов
   - **📐 Соотношение** → выбираешь из 4 вариантов
   - **🎲 Seed** → выбираешь "случайный" или "фиксированный", если фиксированный — вводишь число
   - **📊 Шаги** → выбираешь из 3 вариантов
   - **🔆 CFG** → выбираешь из 3 вариантов
   - **🔄 Сбросить** → возвращает все на значения по умолчанию

4. После выбора → сохраняется в БД
5. Видишь обновленные настройки

---

## 📊 Техническое устройство

### База данных

Настройки хранятся в таблице `generation_settings`:

```sql
CREATE TABLE generation_settings (
    user_id INTEGER PRIMARY KEY,
    quality TEXT DEFAULT 'balanced',
    aspect TEXT DEFAULT '1:1',
    seed_mode TEXT DEFAULT 'random',
    seed_value INTEGER DEFAULT 0,
    steps INTEGER DEFAULT 20,
    cfg REAL DEFAULT 7.5,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE
)
```

### Методы репозитория

**storage/repository.py**:

```python
# Получить все настройки пользователя
settings = await repo.get_generation_settings(user_id)
# Результат: {"quality": "balanced", "aspect": "1:1", ...}

# Обновить одну настройку
await repo.update_generation_setting(user_id, "quality", "high")

# Сбросить на значения по умолчанию
await repo.reset_generation_settings(user_id)
```

### Обработчики (handlers)

**bot/handlers/settings.py** содержит:

- `cb_settings()` — главное меню настроек
- `cb_set_quality()`, `cb_quality_selected()` — управление качеством
- `cb_set_aspect()`, `cb_aspect_selected()` — управление соотношением
- `cb_set_seed()`, `cb_seed_random()`, `cb_seed_fixed()`, `msg_seed_value()` — управление seed
- `cb_set_steps()`, `cb_steps_selected()` — управление шагами
- `cb_set_cfg()`, `cb_cfg_selected()` — управление CFG
- `cb_reset_settings()` — сброс

---

## 💡 Советы по использованию

### Для портретов:
```
Качество: 🎨 Высокое (или ⚖️ Баланс)
Соотношение: 4:5 (портрет)
Seed: 🎲 Случайный (или фиксированный для повторов)
Шаги: 20-28
CFG: 7.5-15.0 (выше CFG = более строго по описанию)
```

### Для пейзажей:
```
Качество: ⚖️ Баланс или 🎨 Высокое
Соотношение: 16:9 (панорама)
Seed: 🎲 Случайный
Шаги: 20
CFG: 7.5 (средний CFG лучше всего)
```

### Для быстрых экспериментов:
```
Качество: ⚡ Быстро
Соотношение: 1:1
Seed: 🎲 Случайный
Шаги: 4
CFG: 7.5
→ Результат за 10 сек!
```

### Для точного копирования результата:
```
Качество: ⚖️ Баланс (или то же самое, что было)
Соотношение: 1:1 (или то же самое)
Seed: 📌 Фиксированный (введи то же число, что было)
Шаги: 20 (или то же самое)
CFG: 7.5 (или то же самое)
Промпт: точный (без изменений!)
→ Получишь идентичный результат!
```

---

## 🔗 Связь с основным потоком

Когда пользователь нажимает кнопку генерации (📸, ⚡, 🎬 и т.п.):

1. Загружаются **сохраненные в БД** настройки
2. Применяются к workflow перед отправкой в ComfyUI
3. Пользователь может увидеть какие параметры использовались в логах

**На будущих шагах:** будет интегрирована генерация с учетом этих параметров.

---

## 🎯 Версия

- **Система настроек:** v1.0 (ШАГ 3)
- **Документация:** 2026-02-07
